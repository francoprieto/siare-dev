# Antes de empezar

- Abrir una ventana de PowerShell

- Ejecutar el siguiente comando:

```bash
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

- Posteriormente descargar el script install.ps1

